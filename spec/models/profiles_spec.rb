RSpec.describe 'Profile' do
  context '' do
    it 'should validate the existence of a username on save' do

    end

    it 'should not allow an existing user without a username to update a profile without adding a username' do

    end

    it 'should allow a username to be changed so long as it is unique' do

    end
  end
end

